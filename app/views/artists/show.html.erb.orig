<div id="artist">
  <%= title artist.name %>

  <div class="header row">
    <div class="photo"><%= artist.decorate.photo %></div>
  </div>

  <div class="row">
    <div class="bio large-8 columns">
      <%= artist.decorate.bio %>
    </div>

    <div class="sidebar large-4 columns">
      <div class="releases row">
        <h2>Latest Releases</h2>

<<<<<<< HEAD
      <ul>
      <% artist.releases.latest.limit(5).each do |release| %>
        <li><%= link_to release.name, release %></li>
      <% end %>
      </ul>
=======
        <ul class="clearing-thumbs">
        <% artist.releases.each do |release| %>
          <li><%= link_to release.decorate.thumbnail, release %></li>
        <% end %>
        </ul>
      </div>

      <div class="photos row">
        <h2>Latest Photos</h2>

        <ul class="clearing-thumbs" data-clearing>
        <% artist.photos.each do |release| %>
          <li><%= link_to photo.decorate.thumbnail, photo %></li>
        <% end %>
        </ul>
      </div>
>>>>>>> Get started on photo gallery component
    </div>
  </div>

  <%= admin_controls 'Artists', resource: artist if admin_signed_in? %>
</div>
<script>
  $('.clearing-thumbs').slick({
  });
</script>
