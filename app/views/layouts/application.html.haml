!!!
%html{:lang => "en"}
  %head
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}/
    %meta{:charset => "utf-8"}/
    %meta{:content => "width=device-width, initial-scale=1.0", :name => "viewport"}/
    = csrf_meta_tags
    = title_tag
    = stylesheet_link_tag     'application'
    = favicon_link_tag
    = javascript_include_tag 'vendor/modernizr', 'application'
    = render 'analytics' unless Rails.env =~ /development|test/
  %body
    %header
      .logo.row
        .large-8.columns
          = image_tag 'logo'
        .large-4.columns
          - %w(twitter facebook spotify soundcloud).each do |service|
            = link_to Rails.configuration.send("#{service}_url"), title: service.titleize do
              = icon service
      %nav.topbar
        %section.top-bar-section
          %ul
            %li= link_to t('.releases'),  releases_path
            %li= link_to t('.artists'),   artists_path
            %li= link_to t('.shop'),      Rails.configuration.shop_url
            %li= link_to t('.podcast'),   podcast_path
    %main
      = yield
