<%= @artist.name %> just released something!

<%= sanitize @release.text_description %>

TRACKS:

<% @release.tracks.map(&:decorate).each do |track| %>
  <%= track.number %>. <%= track.title %> (<%= track.short_url %>)
<% end %>

Feel free to download any of them!

---
Wax Poetic Records
