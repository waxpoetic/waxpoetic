staging:
  active: <%= Rails.env.staging? %>
  analytics_id: <%= WaxPoetic.secrets.google_analytics_id %>

production:
  active: <%= Rails.env.production? %>
  analytics_id: <%= WaxPoetic.secrets.google_analytics_id %>
